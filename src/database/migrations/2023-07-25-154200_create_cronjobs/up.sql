CREATE TABLE IF NOT EXISTS cronjobs ();

ALTER TABLE cronjobs
  ADD COLUMN id UUID PRIMARY KEY NOT NULL,
  ADD COLUMN schedule VARCHAR(10) NOT NULL,
  ADD COLUMN service  VARCHAR(10) NOT NULL,
  ADD COLUMN status   VARCHAR(10) NOT NULL,
  ADD COLUMN route    VARCHAR(60) NOT NULL,
  ADD COLUMN since    TIMESTAMP WITH TIME ZONE,
  ADD COLUMN until    TIMESTAMP WITH TIME ZONE;
